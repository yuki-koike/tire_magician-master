<!DOCTYPE html>
<html>
<head>
  <meta charset='utf-8'>
</head>
<body>
  <!-- header -->
  <%= render 'sizes/header' %>

  <main>
    <div class="container">
      <div class="row">
        <div class="col-md-4">
          <%= attachment_image_tag @size.manufacture, :image, :size => "350x250" %>
        </div>
        <div class="col-md-8">
          <% if @size.manufacture.tire_maker_id == '1' %>
            <h4>BRIDGESTONE</h4>
          <% elsif @size.manufacture.tire_maker_id == '2' %>
            <h4>DUNLOP</h4>
          <% elsif @size.manufacture.tire_maker_id == '3' %>
            <h4>YOKOHAMA</h4>
          <% end %>
          <p>商品名：<%= @size.manufacture.name %></p>
          <p>サイズ：<%= @size.name.sub(/_/, '/') %></p>
          <p>価格（4本）：<%= @size.price %>円</p>
          <% if user_signed_in? %>
              <%= link_to 'この商品を予約する', size_reserves_new_path(@size.id), class: 'btn btn-danger', :style=> "text-decoration: none; color: white;"  %>
          <% else %>
              <%= link_to '会員登録して予約する', size_reserves_new_path(@size.id), class: 'btn btn-warning', :style=> "text-decoration: none; color: white;" %>
          <% end %>
        </div>
      </div>
    </div>
  </main>

  <!-- footer -->
  <%= render 'sizes/footer' %>

</body>
</html>